# Homework #2 — the semantic retrieval layer.
#
# Homework #1's scripts/prepare_knowledge_base.py remains standard-library only and needs none of
# these; it runs on a bare Python >= 3.9 interpreter. Everything below is required only to embed
# chunks and query the vector index.
openai==2.46.0
chromadb==1.5.9

# Development only.
pytest==9.1.1

# Not listed above and deliberately so: `jsonschema` validates data/processed/chunks.jsonl against
# docs/homework1/assets/chunk.schema.json, but docs/homework1/pipeline-spec.md scopes it to a
# throwaway venv rather than the graded dependency set. Install it ad hoc when you want to re-run
# that check:  pip install jsonschema
